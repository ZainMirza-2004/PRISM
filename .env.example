# Copy this file to ".env" in the same folder and paste your token on the right.
# The real ".env" file is listed in .gitignore — do not commit it.
HF_API_TOKEN=
# OpenRouter API key — dataset scripts (see scripts/generate_openrouter_eval_*.py)
OPENROUTER_API_KEY=
# Optional: override model (default anthropic/claude-sonnet-4.5 for v2)
OPENROUTER_MODEL=
